{
  "name": "extension",
  "version": "0.4.1",
  "private": true,
  "description": "MIP RUM — extension navigateur (2e capteur RUM : injection du SDK par domaine enregistré)",
  "scripts": {
    "build": "node build.mjs",
    "pack": "node build.mjs && node pack.mjs",
    "pack:store": "node build.mjs && node pack.mjs --store",
    "gen:icons": "node icons/generate.mjs",
    "check:sync": "node scripts/check-sync.mjs",
    "smoke": "node scripts/smoke.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "esbuild": "0.28.0",
    "typescript": "5.9.3"
  }
}
