{
  "name": "extension",
  "version": "0.1.0",
  "private": true,
  "description": "MIP RUM — extension navigateur (2e capteur RUM, POC Ext-B)",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "esbuild": "0.28.0",
    "typescript": "5.9.3"
  }
}
